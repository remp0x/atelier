[toolchain]
anchor_version = "0.31.1"

[features]
resolution = true
skip-lint = false

# Placeholder program id (valid Anchor example key). After the first
# `anchor build`, run `anchor keys sync` to replace this everywhere with the
# real keypair generated under target/deploy/. See DEPLOY_AUDIT_RUNBOOK.md.
[programs.localnet]
atelier_staking = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[programs.devnet]
atelier_staking = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
